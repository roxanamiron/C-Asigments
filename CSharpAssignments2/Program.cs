using System;
using System.Collections.Generic;
using System.Collections.Specialized;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace CSharpAssignments2
{
    class Program
    {
        static void Main(string[] args)
        {
            //2. https://www.codewars.com/kata/next-bigger-number-with-the-same-digits 
            //You have to create a function that takes a positive integer number and returns the next bigger number formed 
            //by the same digits;
            //If no bigger number can be composed using those digits, return -1;

            Console.WriteLine("Positive integer number to return the next bigger number formed by the same digits");
            //string stringNumber = "2017";
            int[] array = { 2, 0, 1, 7 };

        

            Console.ReadLine();



        }
    }
}
